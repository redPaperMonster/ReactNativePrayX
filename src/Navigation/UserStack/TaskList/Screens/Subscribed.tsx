import React from 'react';
import {Text, View} from 'react-native';
import {SubsProps} from '../../../ScreensTypes';

const Subscribed: React.FC<SubsProps> = ({navigation, route}) => {
  return (
    <View>
      <Text>nothing(</Text>
    </View>
  );
};

export default Subscribed;
